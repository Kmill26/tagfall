const input=document.querySelector('#deal-search'),toggle=document.querySelector('.search-toggle'),row=document.querySelector('.search-row');if(toggle&&input){toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')!=='true';toggle.setAttribute('aria-expanded',open);row.classList.toggle('is-open',open);if(open)input.focus()});input.addEventListener('input',()=>document.querySelectorAll('.deal-card').forEach(card=>card.hidden=!card.dataset.title.includes(input.value.toLowerCase())))}const sort=document.querySelector('#sort-deals');if(sort){sort.addEventListener('change',()=>{const grid=document.querySelector('#deal-grid');[...grid.children].sort((a,b)=>sort.value==='discount'?b.dataset.discount-a.dataset.discount:sort.value==='price'?a.dataset.price-b.dataset.price:a.dataset.rank-b.dataset.rank).forEach(card=>grid.append(card))})}
