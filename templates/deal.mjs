import { card } from './card.mjs';
import { checkedAt, discount, escapeHtml, money, productImage } from './layout.mjs';
export const deal = ({ deal, category, related, categories, affiliateTag }) => { const pct = discount(deal); const product = { '@context': 'https://schema.org', '@type': 'Product', name: deal.title, sku: deal.asin, offers: { '@type': 'Offer', price: deal.price, priceCurrency: 'USD', availability: deal.status === 'live' ? 'https://schema.org/InStock' : 'https://schema.org/OutOfStock', url: `https://www.amazon.com/dp/${deal.asin}?tag=${affiliateTag}` } }; return { jsonLd: `<script type="application/ld+json">${JSON.stringify(product).replace(/</g, '\\u003c')}</script>`, body: `<section class="shell breadcrumb"><a href="/">Deals</a><span>›</span><a href="/c/${category.slug}/">${escapeHtml(category.name)}</a></section><section class="shell deal-detail"><div class="detail-image">${productImage(deal)}${pct >= 20 ? `<span class="price-tag">${pct}% off</span>` : ''}</div><div class="detail-copy"><p class="eyebrow">${escapeHtml(category.name)}</p><h1>${escapeHtml(deal.title)}</h1><div class="price-block price-block--detail"><strong>${money(deal.price)}</strong>${deal.listPrice ? `<s>${money(deal.listPrice)}</s><span>${pct}% off</span>` : '<span>Everyday price</span>'}</div>${deal.summary ? `<p class="detail-summary">${escapeHtml(deal.summary)}</p>` : ''}<p class="checked">Checked ${checkedAt(deal.verifiedAt)}</p>${deal.status === 'live' ? `<a class="button" href="https://www.amazon.com/dp/${deal.asin}?tag=${affiliateTag}" rel="nofollow sponsored">View on Amazon</a><p class="affiliate-line">As an Amazon Associate I earn from qualifying purchases.</p>` : '<span class="button button--ended">Deal ended</span>'}<p class="price-warning">Prices change — confirm on Amazon before buying.</p></div></section><section class="shell pros-grid"><div class="pros-panel"><h2><span class="icon-check">✓</span>Worth it if</h2><ul>${deal.worthIt.map((item) => `<li>${escapeHtml(item)}</li>`).join('')}</ul></div><div class="pros-panel pros-panel--skip"><h2><span class="icon-cross">×</span>Skip if</h2><ul>${deal.skipIf.map((item) => `<li>${escapeHtml(item)}</li>`).join('')}</ul></div></section><aside class="shell risk-note"><strong>⚠ Risk note</strong><p>${escapeHtml(deal.riskNote)}</p></aside><section class="shell section"><div class="section-heading"><div><p class="eyebrow">Keep browsing</p><h2>More in ${escapeHtml(category.name)}</h2></div><a href="/c/${category.slug}/">See all <span aria-hidden="true">→</span></a></div><div class="deal-grid">${related.map((item) => card(item, categories)).join('')}</div></section>` }; };
